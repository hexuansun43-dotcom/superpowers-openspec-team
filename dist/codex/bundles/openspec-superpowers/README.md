# Codex Bundle: OpenSpec + Superpowers

Install this bundle when you want one standalone Codex skill for the full clarification -> OpenSpec -> implementation -> verification flow.

Install target:

```text
.codex/skills/
```

After installation, invoke:

```text
Use $openspec-superpowers-workflow to run this feature from clarification through verification.
```
